query Entry ($eid: Int!) {
  entry(eid: $eid) {
    children {
      childCount
      content
      eid
      expanded
      name
      parentEid
      rendered
    }
    childCount
    content
    eid
    expanded
    name
    parentEid
    rendered
  }
}
